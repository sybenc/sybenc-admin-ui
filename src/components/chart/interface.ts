//所有图表组件数据项字段格式化
interface ChartDataItem {
    name: string | number,
    value: number
}

//所有图表组件距离边框距离的属性格式化
interface ChartMargin {
    top: number,
    right: number,
    bottom: number,
    left: number
}